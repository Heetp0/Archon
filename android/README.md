# Archon Android APK

Android application build configuration for Archon.

## Build APK

```bash
cd android
./gradlew assembleDebug
```
Output APK location: `android/app/build/outputs/apk/debug/app-debug.apk`
